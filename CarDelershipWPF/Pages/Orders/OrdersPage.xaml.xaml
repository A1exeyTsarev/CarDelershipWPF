<Page x:Class="CarDelershipWPF.Pages.OrdersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Управление заказами"
      Background="#1a1a1a">

    <Page.Resources>
        <!-- Стиль для ComboBox -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#FFC107"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="5"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFC107"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#FFC107"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль для TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFC107"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <!-- Стиль для кнопки -->
        <Style TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <!-- Стиль для ListView -->
        <Style TargetType="ListView">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        </Style>

        <Style TargetType="ListViewItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <ContentPresenter/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="◀ Назад" 
                    Background="#333333" Foreground="#FFC107"
                    BorderBrush="#FFC107" BorderThickness="1"
                    Height="35" Width="100" Click="BtnBack_Click" Cursor="Hand"/>

            <TextBlock Grid.Column="1" Text="Управление заказами" FontSize="24" FontWeight="Bold" 
                       Foreground="#FFC107" HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <TextBlock Grid.Column="2" Width="100" Visibility="Hidden"/>
        </Grid>

        <!-- Панель управления -->
        <Border Grid.Row="1" Background="#2a2a2a" CornerRadius="10" Padding="10" Margin="0,0,0,10"
                BorderBrush="#404040" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="txtSearch" Grid.Column="0" Tag="Поиск по номеру..." 
                         TextChanged="txtSearch_TextChanged"/>

                <ComboBox x:Name="cmbStatus" Grid.Column="1" Width="150" Margin="10,0" 
                          SelectionChanged="cmbStatus_SelectionChanged"/>

                <ComboBox x:Name="cmbSort" Grid.Column="2" Width="150" Margin="0,0,10,0" 
                          SelectionChanged="cmbSort_SelectionChanged"/>

                <Button x:Name="btnAdd" Grid.Column="3" Content="➕ Создать заказ" Width="120" Height="35"
                        Background="#FFC107" Foreground="Black" FontWeight="Bold"
                        Click="btnAdd_Click" Margin="0,0,10,0" Cursor="Hand"/>

                <Button x:Name="btnReset" Grid.Column="4" Content="Сбросить" Width="100" Height="35"
                        Background="#333" Foreground="#FFC107" FontWeight="Bold"
                        Click="btnReset_Click" Cursor="Hand"/>
            </Grid>
        </Border>

        <!-- Список заказов через ListView -->
        <ListView x:Name="lvOrders" Grid.Row="2" Margin="0,0,0,0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Background="#2a2a2a" CornerRadius="10" Padding="15"
                            BorderBrush="#FFC107" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Информация о заказе -->
                            <StackPanel Grid.Column="0" Width="50">
                                <TextBlock Text="{Binding Order_Id}" FontSize="14" FontWeight="Bold" 
                                           Foreground="#888" TextAlignment="Center"/>
                                <TextBlock Text="ID" FontSize="10" Foreground="#555" TextAlignment="Center"/>
                            </StackPanel>

                            <!-- Основная информация -->
                            <StackPanel Grid.Column="1" Margin="15,0">
                                <TextBlock FontSize="16" FontWeight="Bold" Foreground="#FFC107">
                                    <Run Text="Заказ №"/><Run Text="{Binding OrderNumber}"/>
                                </TextBlock>

                                <WrapPanel Margin="0,8,0,0">
                                    <TextBlock Foreground="#888" Margin="0,0,15,0">
                                        <Run Text="📅 "/><Run Text="{Binding CreatedDate}"/>
                                    </TextBlock>
                                    <TextBlock Foreground="#888" Margin="0,0,15,0">
                                        <Run Text="👤 "/><Run Text="{Binding UserName}"/>
                                    </TextBlock>
                                    <TextBlock Foreground="#FFC107" FontWeight="Bold" Margin="0,0,15,0">
                                        <Run Text="💰 "/><Run Text="{Binding TotalAmount, StringFormat={}{0:N0}}"/> ₽
                                    </TextBlock>
                                    <TextBlock FontWeight="Bold">
                                        <Run Text="📌 "/>
                                        <Run Text="{Binding StatusName}" Foreground="#66B3FF"/>
                                    </TextBlock>
                                </WrapPanel>
                            </StackPanel>

                            <!-- Кнопки действий -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                <Button Content="✏️" Width="35" Height="30" 
                                        Click="btnEditStatus_Click" Tag="{Binding}" 
                                        Background="#FFC107" Foreground="Black" 
                                        Cursor="Hand" ToolTip="Изменить статус"
                                        Margin="0,0,5,0"/>

                                <Button Content="🖨️" Width="35" Height="30" 
                                        Click="btnPrintReceipt_Click" Tag="{Binding}" 
                                        Background="#28A745" Foreground="White" 
                                        Cursor="Hand" ToolTip="Печать чека"
                                        Margin="0,0,5,0"/>

                                <Button Content="📱" Width="35" Height="30" 
                                        Click="btnShowQR_Click" Tag="{Binding}" 
                                        Background="#17A2B8" Foreground="White" 
                                        Cursor="Hand" ToolTip="Показать QR-код"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>